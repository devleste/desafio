import styled from 'styled-components';

export const Container = styled.div`
  position: absolute;
  left: 0;
  bottom: 0;
  padding: 40px 20px;
  overflow: hidden;
  @media (max-width: 1024px) {
    padding-bottom: 56px;
  }
`;